package isel.leic.ps.Services

import isel.leic.ps.DTO.ObserverDTO
import org.junit.Test
import org.junit.runner.RunWith
import org.springframework.test.context.junit4.SpringRunner

import org.junit.Assert.assertEquals

@RunWith(SpringRunner::class)
class ObserverServiceTest {

    @Test
    public fun getObserverByNumber(): Unit {
        // Arrange
//        var observerSvc = ObserverService(ObserverMemoryDAL())
//        var expected = ObserverDTO("TesteUser", 999, "testmail", "testeavatar@com")
//        observerSvc.createObserver(expected)
//        var obtained = observerSvc.getObserverByNumber(999)
//        assertEquals(expected.name, obtained.name)
//        assertEquals(expected.number, obtained.number)
    }


}